import { FC } from 'react'
import styles from "../styles/profile_picture.module.css"

const ProfilePicture: FC = () => { 
    return <div className={styles.profile_picture_container}></div>
    
}

export default ProfilePicture; 